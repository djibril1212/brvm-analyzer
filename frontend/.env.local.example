# URL du backend FastAPI (Railway en prod, localhost:8000 en dev)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Secret pour le webhook de revalidation ISR
# Doit correspondre à NEXT_REVALIDATION_SECRET dans le backend
NEXT_REVALIDATION_SECRET=your-revalidation-secret-here
